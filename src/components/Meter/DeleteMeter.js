import React from 'react';
import { useParams } from "react-router-dom";

export default function DeleteMeter() {
    let { id } = useParams();
    return (
        <div>
            "Hoi"
        </div>
    )
}